---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: trino
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - list:
        elements:
          - cluster: in-cluster
  template:
    metadata:
      name: trino
    spec:
      destination:
        name: "{{cluster}}"
        namespace: trino
      project: default
      source:
        path: manifests/workloads/trino
        repoURL: https://github.com/dormstd/k8s-dataplatform-poc.git
        targetRevision: HEAD
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true